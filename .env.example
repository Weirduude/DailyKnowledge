# DailyKnowledge Environment Configuration
# Copy this file to .env and fill in your values

# ==================== LLM API (OpenAI Compatible) ====================
# Required: Your API Key (sensitive, never commit!)
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Custom endpoint (for Azure, local models, etc.)
# Default: https://api.openai.com/v1
# OPENAI_BASE_URL=https://your-custom-endpoint.com/v1

# Optional: Model name (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_MODEL=gpt-4o
# OPENAI_MODEL=deepseek-chat

# ==================== Email (SMTP) ====================
# 
# Gmail 配置：
#   SMTP_SERVER=smtp.gmail.com
#   SMTP_PORT=587 (STARTTLS)
#   SMTP_PASSWORD=16位应用专用密码 (需在 Google 账户开启两步验证)
#
# QQ 邮箱配置：
#   SMTP_SERVER=smtp.qq.com
#   SMTP_PORT=465 (SSL) 或 587 (STARTTLS)
#   SMTP_PASSWORD=授权码 (在 QQ 邮箱设置中生成)
#
# 163 邮箱配置：
#   SMTP_SERVER=smtp.163.com
#   SMTP_PORT=465 (SSL)
#   SMTP_PASSWORD=授权码

SMTP_SERVER=smtp.qq.com
SMTP_PORT=465
SMTP_USERNAME=your_qq_number@qq.com
SMTP_PASSWORD=your_authorization_code

# Sender and recipient addresses
EMAIL_FROM=your_qq_number@qq.com
EMAIL_TO=recipient@example.com
